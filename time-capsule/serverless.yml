org: dohyeon5626
app: time-capsule
service: time-capsule

provider:
  name: aws
  runtime: nodejs20.x

functions:
  api:
    handler: app.application
    events:
      - httpApi: "*"

plugins:
  - serverless-offline
  - serverless-dotenv-plugin

package:
  exclude:
    - .env